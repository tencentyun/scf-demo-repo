Resources:
  default:
    Type: TencentCloud::Serverless::Namespace
    Cos_Merge_File:
      Type: TencentCloud::Serverless::Function
      Properties:
        CodeUri: ./
        Description: 本範例使用 COS 觸發器，完成CDN快取重新整理動作
        Environment:
          Variables:
        Handler: index.main_handler
        MemorySize: 256
        Runtime: Nodejs8.9
        Timeout: 900